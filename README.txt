Garf Detector front end.


To install, create a new venv and "pip install -r requirements.txt" 
Then, register a free trial at confluent cloud and get a client.properties file and put it in the project folder. 
Activate your venv and run garf-producer.py to start detecting garfields (and people)!